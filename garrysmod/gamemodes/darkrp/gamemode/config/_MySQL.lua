RP_MySQLConfig = {
	EnableMySQL = true,
	Preferred_module = 'mysqloo',
	Host = CFG.db.host,
	Username = CFG.db.user,
	Password = CFG.db.pass,
	Database_port = CFG.db.port,
	Database_name = CFG.db.main,
	Database_socket = CFG.db.socket,
}
