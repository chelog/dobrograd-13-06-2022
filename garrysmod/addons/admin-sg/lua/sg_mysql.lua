SERVERGUARD.MySQL = {
	enabled = 1,
	module = 'mysqloo',
	unixsocket = CFG.db.socket,
	host = CFG.db.host,
	username = CFG.db.user,
	password = CFG.db.pass,
	port = CFG.db.port,
	database = CFG.db.admin,
}
